{
  "name": "illinois-tech-skill-tree",
  "version": "1.0.0",
  "description": "Illinois Tech Academic Skill Tree - Course Planning Visualization",
  "private": true,
  "scripts": {
    "start": "PORT=3003 react-scripts start",
    "backend": "cd backend && npm run start",
    "dev": "PORT=3003 react-scripts start",
    "prebuild": "node scripts/write-build-env.js",
    "build": "react-scripts build",
    "build:serve": "npm run build && npx serve -s build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "openai": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.8",
    "tailwindcss": "^3.4.19"
  }
}
